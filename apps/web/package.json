{
  "name": "@cadena/web",
  "version": "0.0.0",
  "private": true,
  "description": "Frontend de CADENA. Vite + TypeScript modular, consume packages/engine y packages/data directo.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/verificar-build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "~7.3.6",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
