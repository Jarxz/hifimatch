{
  "name": "@cadena/web",
  "version": "0.0.0",
  "private": true,
  "description": "Frontend de CADENA. Vite + TypeScript modular, consume packages/engine y packages/data directo.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config vite.ar.config.ts && node scripts/verificar-build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"src/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "@types/webxr": "^0.5.24",
    "typescript": "^5.6.0",
    "vite": "~7.3.6",
    "vite-plugin-singlefile": "^2.3.3"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
