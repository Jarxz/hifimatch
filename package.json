{
  "name": "cadena",
  "private": true,
  "description": "CADENA — motor de compatibilidad hi-fi. Monorepo: packages/engine (motor), packages/data (catálogo bilingüe), apps/web (frontend).",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "test": "npm test -w packages/engine",
    "typecheck": "npm run typecheck -w packages/engine",
    "verify": "npm run typecheck && npm run test"
  }
}
