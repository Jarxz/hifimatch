{
  "name": "@cadena/engine",
  "version": "0.0.0",
  "private": true,
  "description": "Motor de compatibilidad hi-fi de CADENA. TypeScript puro, cero dependencias de runtime.",
  "type": "module",
  "scripts": {
    "test": "node --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json && node scripts/bundle-navegador.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0"
  }
}
