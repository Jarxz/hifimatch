{
  "name": "@cadena/contact",
  "version": "0.0.0",
  "private": true,
  "description": "Validación pura del formulario de contacto de The Hifi Match. TypeScript puro, cero dependencias de runtime — consumida tanto por apps/web (feedback instantáneo en el cliente) como por api/contact.ts (el borde de seguridad real).",
  "type": "module",
  "scripts": {
    "test": "node --test \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0"
  }
}
