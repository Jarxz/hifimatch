node_modules/
.DS_Store

# apps/web/dist/ es el build de Vite (vite-plugin-singlefile): un artefacto
# generado, no un archivo commiteado. Se genera con `npm run build` y Vercel
# lo reconstruye en el deploy.
apps/web/dist/
.vercel/
